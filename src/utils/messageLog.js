export const messageLog = (message) => {
    //eslint-disable-next-line no-console
    console.log(message);
};

export const errorMessageLog = (error) => {
    //eslint-disable-next-line no-console
    console.error(error);
}

export default messageLog;
